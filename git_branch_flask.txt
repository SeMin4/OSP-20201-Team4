2018114505 서 진주
git branch : flask

goal : 웹 페이지 crawling 한뒤 빈도수 계산하여 DB에 저장하기

1. web crawling 후 단어, 빈도수 check
- 'body' 부분 text 크롤링
- 숫자 및 특수문자 제거 
	--> del_symbols 함수 , return값 : 숫자 및 특수문자를 제거한 text list
- stopwords 제거 
	--> del_stopwords 함수, return값 : stopwords를 제거한 text list
- word_d 딕셔너리 만들기


2. 처리시간 계산
process_timer() 함수 return : 현재 시간
process_tiem = end - start

3. json 형식으로 elasticsearch에 저장.
index = 'urls', doc_type = 'url' ,id

단어빈도수 세기 : word_d 딕셔너리에 word추가


#. tf_idf 계산하여 주요 top 10 단어 리스트 make
여러 page의 내용(string)을 하나의 list에 추가 --> sent_list

- tf값 계산
	--> compute_tf 함수
- idf 값 계산
	--> compute_idf함수
-->이것을 하려면 DB에일단 데이터가 저장되어야함 --> 추후 
